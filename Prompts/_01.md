You are working in this existing public GitHub repository:

https://github.com/vunvulear/DevOpsPro-Europe-2026

This repository is used for the conference talk:
“Platform Engineering in 30 Minutes: Build a Golden Path, Not a PowerPoint”

The repository already contains a Node.js/Express application. Do not replace the application.

Your task is to install and configure the spec-driven development foundation for the repo, using GitHub Spec Kit and useful extension workflows.

Important:
- Use the official GitHub Spec Kit repository only:
  https://github.com/github/spec-kit
- Do not install unofficial PyPI packages named `specify-cli`, `speckit`, `spec-kit`, or similar.
- Prefer the official GitHub-based command:
  `uvx --from git+https://github.com/github/spec-kit.git specify init .`
- If `uv` or `uvx` is missing, stop and tell me the exact command I need to run to install it.
- Keep everything suitable for a public GitHub repository.
- Do not add secrets, tokens, Azure credentials, subscription IDs, tenant IDs, or client IDs.
- Optimize for a 30-minute live demo, not for a production-grade internal platform.

Please do the following:

1. Inspect the repository structure.

2. Check whether Spec Kit is already initialized.
   Look for:
   - `.specify/`
   - `specs/`
   - `.github/prompts/`
   - command files
   - agent instruction files
   - existing spec/plan/task files

3. If Spec Kit is not initialized, initialize it in the current repository using:

   `uvx --from git+https://github.com/github/spec-kit.git specify init .`

4. Configure the repo for Windsurf/Cascade usage as much as Spec Kit supports.
   If Windsurf is not available as a direct agent option, choose the closest compatible option and document the decision.

5. Verify the installation, if possible, using:

   `specify version`

   or the equivalent command available after initialization.

6. Add or prepare useful Spec Kit extension-style workflows for this demo.

   Use this repository as reference:
   https://github.com/MartyBonacci/spec-kit-extensions

   The useful workflows for this demo are:
   - `modify`
   - `refactor`
   - `hotfix`
   - `bugfix`

   Do not blindly copy the whole external repository.
   Add only the workflow prompts/instructions needed for this demo.
   Keep them short, readable, and suitable for Windsurf/Cascade.

7. Create a local folder for demo prompts if it does not exist:

   `prompts/`

   Add these files:

   - `00-install-and-configure-speckit.md`
   - `01-create-golden-path-spec.md`
   - `02-create-implementation-plan.md`
   - `03-create-task-breakdown.md`
   - `04-add-ci.md`
   - `05-add-security-scanning.md`
   - `06-add-terraform-azure-app-service.md`
   - `07-add-terraform-plan-workflow.md`
   - `08-add-azure-app-service-deploy.md`
   - `09-add-platform-policies.md`
   - `10-add-cost-awareness.md`
   - `11-add-operational-readiness.md`
   - `12-update-readme.md`
   - `13-create-demo-flow.md`
   - `14-create-hotfix-demo.md`
   - `15-final-review.md`

   If the content for these prompts is not available yet, create placeholder files with TODOs.

8. Create the initial Spec Kit/golden-path folder if it does not exist:

   `specs/golden-path/`

9. Add a short documentation file:

   `platform/docs/spec-kit-setup.md`

   Explain:
   - how Spec Kit was initialized
   - which agent configuration was selected
   - which extension workflows were added
   - how Windsurf/Cascade should be used with this repo
   - what is intentionally simplified for the demo

10. Add a short section to the root README:

   “Spec-driven demo workflow”

   It should explain:
   - Spec Kit is used to structure the work
   - Windsurf/Cascade is used to implement the steps
   - GitHub Actions validates the golden path
   - Azure App Service is the deployment target

11. Do not over-engineer.
    Prefer clarity and explainability over completeness.

At the end, summarize:
- whether Spec Kit was already present or initialized
- commands run
- extension workflows added or prepared
- files added or changed
- assumptions made
- what I should review before committing